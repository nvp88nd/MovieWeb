<div class="container py-4">
    <h2>
        <%= movie.title %>
    </h2>
    <p class="text-muted">
        <%= movie.origin_name %> | <%= movie.status %>
    </p>

    <div class="ratio ratio-16x9 mb-4">
        <iframe src="<%= currentEpisode.video_url %>" allowfullscreen></iframe>
    </div>

    <div class="mb-3">
        <h5>Tập hiện tại: <%= currentEpisode.episode_name %>
        </h5>
        <div class="d-flex flex-wrap gap-2 mt-2">
            <% episodes.forEach(ep=> { %>
                <a href="/xem-phim/<%= movie.slug %>/<%= ep.episode_name.replace('Tập ', '') %>"
                    class="btn btn-sm <%= ep.episode_name === currentEpisode.episode_name ? 'btn-success' : 'btn-secondary' %>"
                    style="width: 60px;">
                    <%= ep.episode_name %>
                </a>
                <% }) %>
        </div>
    </div>

    <div class="mt-4">
        <h5>Bình luận</h5>
        <p>
            <%= movie.description %>
        </p>
    </div>
</div>